let root;export function mount(r){root=r}export function update(d){const row=(a,b,c,n)=>'<div class="summary-row"><div><strong>'+a+'</strong><small>'+n+'</small></div><div>'+b+'</div><div>'+c+'</div></div>';root.innerHTML='<div class="summary-grid">'+row('Theoretical stages / transfer units',d.N.toFixed(3),d.NOG.toFixed(3),'N vs N_OG')+row('HETP / H_OG',d.HETP.toFixed(3)+' m',d.HOG.toFixed(3)+' m','Height-transfer link')+row('Tray height / packed height',d.ZTray.toFixed(3)+' m',d.Z.toFixed(3)+' m','Installed height')+row('Actual trays',d.nActual,'continuous packed height','Discrete vs continuous')+row('L / V',d.LoV.toFixed(3),'minimum '+d.LoVmin.toFixed(3),'Derived from endpoints')+row('L / V ÷ minimum',(d.LoV/d.LoVmin).toFixed(3),'','')+row('Liquid flow L',d.L.toFixed(3)+' kmol/h','V = '+d.inputs.V.toFixed(3)+' kmol/h','L is derived, not an input')+row('Absorption factor A',d.A.toFixed(4),'A_min '+d.Amin.toFixed(4),'A < 1 can be feasible')+row('Consistency check',(d.N*d.HETP).toFixed(6)+' m',d.Z.toFixed(6)+' m','N × HETP = Z is a definition-based consistency check')+'</div>'}